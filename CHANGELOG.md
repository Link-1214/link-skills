# 변경 이력

버전은 각 플러그인의 매니페스트에 적혀 있고 직접 올립니다. 릴리스할 때만 바뀝니다.
고침은 patch, 새 동작은 minor, 호출 이름이나 산출물 형식이 바뀌면 major 입니다.

## link-design-pitch

### 1.3.0

스스로에게 적대적 오딧을 돌려서 나온 것들을 고쳤습니다. 여덟 개 중 여섯은 「글로는 맞는데 실행이
따라오는지 검증되지 않은」 부류였습니다.

**색인 절감이 실행에서도 성립하게 만들었습니다.** 「고른 열 개만 읽어라」는 지시만 있고 방법이
없었습니다. 파일을 여는 도구는 대개 통째로 돌려주므로, 그대로면 절감은 측정에만 존재합니다.
항목 제목(`## <번호>.`)을 검색해 그 줄 범위만 읽으라는 방법을 색인 옆과 본문 양쪽에 적었고, 줄
범위를 못 읽는 환경에서는 「한 번만 읽고 두 번 읽지 말라」로 후퇴하게 했습니다.

**질문 3이 아직 없는 답에 의존하던 모순을 풀었습니다.** 「다섯을 한 번에 물어라」와 「답 1·2에서
팔레트를 도출하라」가 같은 절차에 있었습니다. 맥락이 답을 담고 있을 때만 우연히 작동했습니다.
이제 팔레트는 맥락 추정으로 만들되 **어느 추정 위에 서 있는지 명시**합니다. 전제가 보이는 팔레트는
답과 같은 호흡에 교정됩니다.

**상세 스킬이 기록이 있는데도 거부하는 경우를 막았습니다.** 고정 경로 두 곳에 없으면 바로
거부했는데, 모노레포 하위나 다른 위치에 기록이 있으면 오작동 거부가 됩니다. 이제 프로젝트 전체를
한 번 찾아보고, 그래도 없으면 위치를 물은 다음에야 거부합니다. 거부 자체는 그대로 남습니다 —
기록이 정말 없으면 방향을 지어내지 않습니다.

**비용 숫자를 정직하게 만들었습니다.** 문서의 절감 수치는 읽기 비용만 센 것이었습니다. 실행
전체는 시안 열 개를 그리고 검증까지 하므로 그 몇 배가 듭니다. README에 그렇게 적었고, 이 스킬은
자주 도는 스킬이 아니라 큰 작업 전에 한 번 돌아 재작업을 없애는 스킬이라는 것도 함께 적었습니다.

**CI를 붙였습니다.** frontmatter의 콜론 함정, 마켓플레이스 등록 누락, 번들 의존성 누락, 이모지 —
전부 실제로 겪었거나 겪으면 조용히 실패하는 것들을 `scripts/validate_repo.py` 가 잡고, 푸시마다
돌아갑니다. 결함을 심은 저장소로 음성 테스트를 해서 일곱 종을 전부 잡는 것까지 확인했습니다.

그 밖에: 카탈로그 개수를 산문 여섯 곳에서 파일당 한 곳으로 줄였습니다(늘 때마다 여섯 곳을 고치다
하나 놓치면 문서가 거짓말을 합니다). PNG 캡처 명령의 Windows 변형을 적었습니다(작성자 본인 기계가
Windows인데 예시가 POSIX 전용이었습니다). 타깃이 브라우저가 아닐 때 보드는 근사라는 것을 보드에
말하게 했습니다. 비슷한 디자인 스킬이 여럿 설치된 환경에서는 이름으로 부르라고 README에
적었습니다.

### 1.2.0

**답에서 무엇이 켜지는지를 읽는 규칙을 넣었습니다.** 새 질문을 더하지 않았습니다. 다섯 개 답에
이미 신호가 들어 있기 때문입니다.

- 5번 답이 「조작하게」가 아니라 「알아채게·이해하게」이면, 화면이 숫자를 나열하는 대신 문장으로
  말해야 합니다. 데이터 스토리텔링이라고 부르는 것이고, 사실 이미 그렇게 하고 있었는데 이름이
  없어서 다음 실행에서는 안 나올 수도 있었습니다.
- 2번이나 5번에 제품이 사용자 대신 행동하는 대목이 있으면, 상세 스킬의 인터랙션 갈림길에
  「제품이 스스로 행동하는가」가 추가됩니다. 작업 중 표시, 제안과 적용의 구분, 제품이 바꾼 것과
  사람이 바꾼 것의 구분, 되돌리기 단위, 거부 사유입니다.
- **해당되지 않으면 그 갈림길은 아예 없습니다.** 대시보드·양식·문서 대부분이 여기 해당하고,
  거기서 이걸 묻는 건 소음입니다.

멀티모달 입력과 적응형 레이아웃은 검토하고 넣지 않았습니다. 전자는 니치이고, 후자는 4번 질문에서
자유롭게 말할 수 있어 따로 이름 붙일 값어치가 없습니다.

**PNG의 용도 설명을 고쳤습니다.** 「비전 되는 AI용」이라고 적어 두었는데 틀렸습니다. GitHub도,
메신저도, 휴대폰도 HTML을 렌더하지 않습니다. **사람이 이 보드를 보는 대부분의 자리가 PNG입니다.**
세 파일이 각자 다른 일을 하고 각자의 일에서 제일 싸다는 것도 함께 적었습니다 — HTML은 브라우저를
가진 사람용, PNG는 그 외 모든 곳, `DECISION.md` 는 재현용입니다.

### 1.1.0

카탈로그를 24개에서 29개로 늘렸습니다. 웹을 다시 훑어 빠진 것을 찾았고, 고전 셋과 2026년
흐름 둘이 나왔습니다.

- **바우하우스** — 원색과 기하 도형. 스위스·플랫·머티리얼의 조상이라 낯설지 않게 읽힙니다.
- **아르데코** — 기하학적 대칭과 금색. 격식이 필요한 자리용입니다.
- **구성주의** — 대각선과 적·흑. 긴급함을 만드는 데 특화돼 있습니다.
- **안티-AI / 핸드메이드** — 종이 질감, 잘린 가장자리, 의도적인 불완전함. 2026년 가장 큰
  흐름이고, AI가 만든 것처럼 보이면 안 되는 자리를 위한 것입니다.
- **자판디 / 따뜻한 미니멀** — 미니멀리즘의 실제 약점인 차가움을 온도로 해결합니다.

플랫 디자인은 검토했지만 넣지 않았습니다. 미니멀리즘·머티리얼·바우하우스 셋이 이미 그 영역을
덮고 있어서, 넣으면 색인만 길어지고 고를 때 헷갈립니다.

**목업을 어떻게 만드는지를 명시했습니다.** 이미지를 검색하거나 가져오지 않고 HTML과 CSS로
직접 그린다는 것. 전에는 산출물 경로와 「외부 요청 없음」 규칙에서 간접적으로만 드러났는데,
문서만 읽고는 헷갈릴 수 있었습니다. 검색해 온 이미지에는 그 프로젝트의 내용이 들어갈 수 없고,
보드가 시험하는 건 정확히 그것입니다.

### 1.0.0

첫 릴리스. 플러그인 하나에 스킬 둘을 넣었습니다. 두 번째는 첫 번째 없이는 의미가 없기 때문입니다.

**`link-design-pitch`** — 질문 다섯 개, 프로젝트의 실제 내용으로 렌더한 시안 열 개, 장단점과 렌더
가능 여부를 표로 정리, 추천 하나와 그 추천에 대한 가장 강한 반론까지. 그리고 **거기서 멈추고 주인의
선택을 묻습니다.**

**`link-design-pitch-detail`** — 정해진 방향을 모든 화면에 적용하고, 위계와 대비를 주장하는 대신
**재고**, 코드에서 뭘 바꿔야 하는지를 목록으로 냅니다. 그다음 인터랙션 선택지를 펼쳐 놓고 묻습니다.
**기록에 정해진 방향이 없으면 실행을 거부합니다.**

데스크톱 대시보드 하나와 웹 칸반보드 하나, 두 번의 실제 작업이 이 스킬의 모양을 잡았습니다.
아래 규칙이 유난히 구체적인 이유는 전부 거기서 나왔기 때문입니다.

- **정지는 필수이고 두 번 적혀 있습니다.** 첫 실행은 아무도 방향을 고르기 전에 상세 명세를 다
  만들어 버렸습니다. 결정 지점을 지나 만든 것은 주인이 버릴 수 있는 작업이고, 동시에 주인에게서
  빼앗은 결정입니다.
- **조합은 절대 열 개 중 하나가 아닙니다.** 초기 두 실행이 모두 조합으로 끝났는데, 지시문에
  「답은 흔히 둘의 조합이다」가 있었기 때문입니다. 조합에 슬롯을 주면 열 칸으로 아홉 개만 덮게 되고,
  빠지는 것은 가장 많이 가르쳐 주는 스트레치 안입니다.
- **위계는 재고, 어느 채널을 쓰는지도 함께 봅니다.** 첫 실행에서는 두 행을 걸친 요소 때문에 네 탭 중
  세 탭의 위계가 조용히 뒤집혀 있었습니다. 두 번째 실행에서는 같은 규칙이 멀쩡한 설계를 결함으로
  신고할 뻔했는데, 그 설계는 위계를 면적이 아니라 대비로 나르고 있었기 때문입니다.
- **대비는 불투명도 합성 뒤에 잽니다.** 남의 카드를 50%로 흐리는 건 세련돼 보이지만 3.5:1 로
  떨어집니다. 이 방식으로 접근성 결함 둘을 잡았고, 그중 하나는 이미 출시된 화면에 있었습니다.
- **5단계는 선택지를 제시하고 만들지 않습니다.** 「낙관적 갱신이냐 확인 후 반영이냐」를 축으로 쓴
  전체 거동 명세가, 아무도 제시하지 않았던 세 번째 답 하나에 한 문장으로 무효가 됐습니다.
- **산출물은 프로젝트가 문서를 두는 곳에 놓고**, 스킬 이름을 딴 폴더는 절대 만들지 않습니다.

이번 릴리스에 함께 들어간 것: 카탈로그 둘 다 앞에 손으로 쓴 색인이 있어 스물넷이 아니라 실제로
쓰는 열 개만 읽습니다(입력의 29% 절감). 보드는 PNG로도 캡처해 비전이 되는 에이전트가 마크업이
아니라 디자인을 실제로 보게 했습니다. `DECISION.md` 는 평문 마크다운이라 결정이 그것을 만든 세션보다
오래 남습니다.

## link

### 1.0.0

전체 설치용 플러그인입니다. 스킬을 직접 담지 않고 나머지 플러그인을 의존성으로 가리킵니다.
한 번 설치하면 전부 들어오고, 새 스킬이 추가되면 업데이트로 받을 수 있습니다.
