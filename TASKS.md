# 진행 중 작업

## 2026-08-18 — 두 디자인 스킬, 실제로 겪은 것만 고침

**상태:** 반영 완료, 미푸시. 브랜치 `fix/design-pitch-minimal` (기준 `e1057ff` = 1.7.0)

### 무엇을 고쳤나 (9건, +56줄)

실제로 돌려봐서 걸린 것과, 카탈로그가 자기 데이터와 어긋난 것만 골랐습니다.

| 파일 | 무엇 | 어떻게 알았나 |
|---|---|---|
| `detail/SKILL.md` | `Status` 필드가 **없는** 기록을 「선택 안 됨」으로 읽어 거부할 수 있었음 | 실제 프로젝트에 돌려봄. 구버전 기록에는 그 필드가 없고, 그게 이 스킬이 가장 자주 받을 입력 |
| `detail/references/spec.md` | 「측정하라」고 하는데 **비브라우저 대상을 재는 수단이 없음** | 같은 실행. Qt 앱을 재는 법이 어디에도 없어 시험자가 직접 지어냄 |
| `directions.md` | `--t2` 대비 보증이 표면 기준인데 무조건처럼 읽힘 — **지면에서 12개 실패, 최저 3.51:1** | 29개 전수 계산 |
| `directions.md` | 굵기가 「하드 의존성 **또는** 실격 사유」 둘을 겸해 양방향 불일치 | `feasibility.md` 의존성 표와 대조 |
| `directions.md` | 말미 표의 「These seven」이 실제 14행, 중복 두 쌍 | 세어봄 |
| `board.md` · `spec.md` | 「Two traps」가 9개, 「Four traps」가 5개 | 세어봄 |
| `feasibility.md` | 스프레드시트를 세 곳에서 초대해놓고 읽을 절이 없음 | Q2 선지·토큰 표·`board.md`와 대조 |
| `feasibility.md` | 마크다운 대상 부재 — 토큰 15개가 하나도 도달 안 하는데 거절 조항이 없음 | 토큰별 판정 |
| `AUTHORING.md` | 커밋 전 절차가 이 환경에 없는 CLI를 안내 | 실제로 막힘. 게이트는 `scripts/validate_repo.py` |

**`pitch/SKILL.md`는 한 줄도 안 건드렸습니다.** 절차 파일이 아니라 참조 파일이 틀렸던 것이라
고칠 자리가 거기였습니다.

### 왜 이만큼만 했나

이 저장소는 **돌려봐서 나온 것으로** 개선돼 왔습니다 — 1.7.0이 「세 대상에 실제로 돌려보고 나온
네 가지」, 1.6.0이 「카탈로그가 자기 접근성 규칙을 어기던 것」.

한 번은 기준 문서에 대고 전수 감사를 해서 45건을 고쳐봤는데, `detail/SKILL.md`가 **+116%**로
불었고 진입 검사가 84줄이 되어 「이 스킬이 무엇을 만드는가」보다 「언제 거부하는가」가 길어졌습니다.
스킬의 목적은 **좋은 방향 열 개를 걸어놓고 소유자가 고민해 고르게 하는 것**이지 관문 통과가
아닙니다. 그 작업은 `fix/design-pitch-audit` 브랜치에 남겨뒀습니다 — 발견 목록으로는 값어치가
있고, 나중에 실제로 겪으면 거기서 꺼내 쓰면 됩니다.

### 남은 것

- **`pitch`를 실제로 돌려본 적이 없습니다.** detail만 두 번 돌렸습니다. 열 개 보드, 채널 붕괴
  측정, 대비 실측, `DECISION.md` — 전부 미검증입니다.
- 버전은 안 올렸습니다. 돌려본 뒤 릴리스하는 것이 이 저장소 방식입니다.

### 시험 중 나온 저장소 밖 발견

`exercises/hive-dashboard-core`의 출하된 다크 팔레트에서 **11px 텍스트가 3.37:1**
(`text_hint #5F726D`), 12px가 4.44:1. 기록의 7.05:1은 HTML 보드에서 잰 값이고 실제 사용 토큰은
그 아래 두 단계였습니다. 이 저장소 일이 아니라 그쪽 일입니다.
