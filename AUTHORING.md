# 새 스킬 추가하기

다른 컴퓨터에서 작업할 나와, 이 저장소를 포크하는 사람을 위한 문서입니다. 여기 적힌 규칙은 전부
실제로 뭔가 깨져서 알게 된 것들입니다. 취향으로 정한 것은 없습니다.

## 용어

| 용어 | 뜻 |
|---|---|
| 마켓플레이스 | 이 저장소 (`link-skills`). `add` 하는 대상 |
| 플러그인 | **설치 단위.** 스킬 여러 개를 담음 |
| 스킬 | **호출 단위.** `SKILL.md` 하나 |

Claude Code 공식 용어를 그대로 씁니다. 명령어에 `plugin` 이 그대로 나오는데 문서에서 다른 말을
쓰면, 읽을 때마다 머릿속에서 바꿔야 합니다.

## 추가 절차 (요약)

새 스킬 하나를 늘리려면 네 가지를 하면 됩니다.

1. `plugins/<플러그인>/` 을 만들고 `.claude-plugin/plugin.json` 과 `skills/<스킬>/SKILL.md` 를 둔다
2. `.claude-plugin/marketplace.json` 의 `plugins` 배열에 등록한다
3. `plugins/link/.claude-plugin/plugin.json` 의 `dependencies` 에 플러그인 이름을 한 줄 넣는다
4. `claude plugin validate` 두 종류를 통과시킨다

**3번을 빠뜨리면 새 스킬이 전체 설치에 안 들어갑니다.** 증상이 조용해서 나중에야 알게 되니
주의하세요. 이미 `link` 를 설치한 사람은 `claude plugin update link@link-skills` 로 받습니다.

기존 플러그인에 스킬만 **더하는** 경우라면 `skills/` 아래 폴더 하나만 만들면 끝입니다.
2번과 3번은 이미 되어 있으니까요.

아래는 각 단계에서 실제로 걸렸던 것들입니다.

## 파일이 놓이는 곳

```
plugins/<플러그인>/
├── .claude-plugin/plugin.json      name · description · version · author · license
├── README.md                       사용법. GitHub에서 보는 사람용
└── skills/<스킬>/
    ├── SKILL.md                    절차 — 스킬이 발동하면 항상 읽힘
    ├── AGENTS.md                   Codex 등 다른 에이전트의 진입점
    └── references/*.md             단계별로 필요할 때만 읽힘
```

플러그인 하나가 스킬 여러 개를 담을 수 있습니다. **한 스킬이 다른 스킬 없이는 의미가 없다면 같은
플러그인에 넣으세요.** 플러그인을 설치하면 그 안의 스킬이 전부 들어오므로, 둘을 반드시 함께
다니게 하는 방법은 이것뿐입니다. 반대로 누군가 하나만 원할 법하면 플러그인을 나눕니다.

만든 플러그인은 `.claude-plugin/marketplace.json` 에 등록하고, `plugins/link/` 의 `dependencies`
배열에 이름을 넣어야 전체 설치에 딸려옵니다.

## 이름

호출은 `<플러그인>:<스킬>` 형태입니다. 여기에 서로 반대로 당기는 제약이 둘 있습니다.

- 플러그인 접두사가 이미 네임스페이스라, 스킬 이름은 짧을수록 읽기 좋습니다.
- 그런데 설치 대신 clone 하는 사람은 **폴더 이름**을 접두사 없이 그대로 받습니다. 스킬 폴더를
  `detail/` 로 지으면 그 사람 기계에서 `/detail` 이 되는데, 그건 누구도 차지하면 안 되는 이름입니다.

그래서 **플러그인 이름과 겹치더라도 스킬 폴더는 온전한 이름으로** 짓습니다.
`link-design-pitch:link-design-pitch-detail` 은 길지만 어느 설치 방식에서도 헷갈리지 않습니다.
조금 긴 것이 이름 충돌보다 낫습니다.

## SKILL.md 쓰기

**frontmatter.** `name` 은 폴더 이름과 같아야 합니다. `description` 은 모델이 이 스킬을 부를지
정할 때 보는 유일한 정보입니다. 무엇을 하는지와 **언제 부를지**를 함께, 약간 밀어붙이듯 쓰세요.
스킬은 과하게 발동하는 경우보다 발동해야 할 때 안 하는 경우가 훨씬 많습니다.

**description 안에 `콜론+공백`을 넣지 마세요.** 따옴표 없는 YAML 스칼라에 그게 들어가면 매핑으로
파싱되고 frontmatter 전체가 조용히 버려집니다 — 그러면 스킬은 메타데이터 없이 로드되고 **영원히
발동하지 않습니다.** 대신 줄표(—)를 쓰세요. 여기서 실제로 일어난 일이고, `claude plugin validate`
가 잡아냈습니다.

**무엇이 아니라 왜를 쓰세요.** 이유를 아는 모델은 지시문에 없는 상황도 판단할 수 있습니다.
「면적을 계산해서 가장 큰지 확인하라」는 시키는 대로만 하게 되지만, 「크기로 강조한다는 건 면적에
대한 주장인데 두 줄을 걸치는 요소가 있으면 조용히 뒤집힌다」는 응용이 됩니다.

**멈추는 단계에는 반드시 이유를 붙이세요.** 멈추고 묻는 지시는 가장 값어치 있으면서 가장 자주
건너뛰어집니다. 지나쳤을 때 무엇을 잃는지 적어 두지 않으면 그냥 지나갑니다. 사용자에게서
가져간 결정과, 버려질 작업 두 가지입니다.

**어떤 편향을 심고 있는지 살피세요.** 「답은 흔히 둘의 조합이다」라는 한 문장 때문에 서로 다른 두
프로젝트가 연달아 조합으로 끝났습니다. 스킬이 매번 같은 결론에 도달한다면 그렇게 만든 문장이
어딘가 있습니다.

## 참조 파일

분량이 있는 내용은 `references/` 에 두고 필요한 단계에서만 읽히게 합니다. `SKILL.md` 에는 그 파일
이름과 어느 단계에서 쓰는지를 적어 둡니다.

**일부만 쓰이는 파일에는 색인을 붙이세요.** 24개짜리 카탈로그를 통째로 읽고 10개를 고르면 세 배를
내는 셈입니다. 고르는 데 필요한 정보만 앞에 색인으로 두고 나머지는 아래로 내립니다. 그리고
**스킬 본문에 「색인을 먼저 읽어라」를 반드시 적으세요.** 이게 없으면 파일 구조만 바뀌고 읽는
양은 그대로입니다.

**정보가 어디에 쓰이는지로 나누세요.** 고를 때는 「무엇에 강한가 / 어디서 무너지나」가 필요하고,
그릴 때는 색상값과 글꼴 방침이 필요합니다. 서로 다른 순간에 쓰이는 정보라 자리를 나눠야 하고,
양쪽에 같은 내용을 중복해 두면 안 됩니다.

**색인은 직접 쓰세요.** 자동 요약은 문장 중간을 자르는데, 하필 판단을 가르는 구절이 잘립니다.
「대비가 구조적으로 0이다」, 「원근이 값의 크기를 왜곡한다」 같은 것들입니다. 그 구절이야말로
그 항목을 색인에 넣은 이유 전부입니다.

## 문서 쓰기

사람이 읽는 문서는 전부 같은 어조로 씁니다. 스킬이 늘어나도 저장소가 한 사람이 쓴 것처럼 읽혀야
합니다.

### 어조

| 규칙 | 왜 |
|---|---|
| **이모지를 쓰지 않습니다** | 제목에 붙은 그림은 정보를 더하지 않고, 검색과 복사에서 방해가 됩니다 |
| **주어를 밝힙니다** | 「걸러집니다」가 아니라 「스킬이 거릅니다」. 누가 하는지 모르면 읽는 사람이 되묻습니다 |
| **단정 뒤에 이유를 붙입니다** | 「A입니다」로 끝나는 문단은 대개 「왜?」를 남깁니다 |
| **가르치지 않습니다** | 「포인트는 하나입니다」 같은 도입을 빼고 사실만 씁니다 |
| **번역투를 피합니다** | 영어로 먼저 생각하고 옮기면 티가 납니다. 처음부터 한국어로 씁니다 |
| **문장을 짧게 끊습니다** | 표와 코드블록으로 옮길 수 있는 것은 문장으로 쓰지 않습니다 |

### 문서마다 담는 것

| 문서 | 독자 | 구조 |
|---|---|---|
| `README.md` | GitHub 방문자 | 소개 → 빠른 시작 → 플러그인이 처음이라면 → 스킬 목록 → 설치 → 어떻게 설계했는가 → 운영 방식 → 언어 |
| `plugins/*/README.md` | 그 플러그인만 보는 사람 | 설치 명령 → 무엇을 하는지 → 어디에 쓰나 → 해결하는 것 → 단계별 설명 → 무엇이 나오나 → 하지 않는 것 → 참고 문서 |
| `AGENTS.md` | 다른 AI | 용어 → 스킬 목록과 진입점 → 붙이는 법 → 지켜야 할 것 → 구조 |
| `CHANGELOG.md` | 버전을 따라오는 사람 | 버전마다 **무엇이 바뀌었고 어느 실패에서 나왔는지** |

**루트 README의 스킬 목록은 형식을 고정합니다.** 스킬이 늘어도 같은 틀로 붙습니다.

```
### <플러그인 이름>
설치 명령 (코드블록)
한 줄 요약 + 호출·역할 표
어디에 쓰나
이 스킬이 해결하는 것
무엇이 나오나
사용법 자세히 → 링크
```

### 설명할 때의 기준

**전문 용어를 쓰되 그 자리에서 풀어 줍니다.** 「도메인 지식」, 「톤앤매너」, 「디자인 시스템」은
그대로 쓰고, 처음 나오는 자리에서 무슨 뜻인지가 문맥으로 드러나게 씁니다.

**비유는 하나만 끝까지 씁니다.** 핸들로 시작했으면 배로 끝내지 않습니다.

**예시는 나쁜 예와 좋은 예를 나란히 둡니다.** 「방향까지 들어가면 좋습니다」보다
「예산안을 검토하게 한다 / 3안이 아니라 2안으로 결정하게 한다」가 한 번에 전달됩니다.

**실패담을 지우지 않습니다.** 「실제로 한 번 그랬습니다」 같은 문장은 규칙이 왜 있는지를
설명하는 가장 짧은 방법입니다.


## 커밋 전에

```bash
claude plugin validate ./plugins/<플러그인> --strict
```

```bash
claude plugin validate .
```

앞의 것은 플러그인 매니페스트와 모든 스킬의 frontmatter를 검사하고, 뒤의 것은 마켓플레이스
카탈로그와 각 `source` 경로가 실제로 있는지 확인합니다. 둘 다 통과해야 합니다. `--strict` 는
경고도 오류로 취급합니다.

그다음 직접 확인할 것:

- `SKILL.md` 가 언급한 `references/*.md` 가 전부 실제로 있는가
- frontmatter의 `name` 이 폴더 이름과 같은가
- 스킬이 사용자 프로젝트에 파일을 만든다면, 폴더 이름이 **스킬 이름이 아니라 그 안에 든 것**으로
  지어졌는가. 남의 저장소 최상위에 `design-pitch/` 가 생기면 누가 만들었는지만 말하고 무엇이
  들었는지는 말하지 않습니다. 이건 나중에 옮겨야 했습니다.

## 버전

`plugin.json` 의 `version` 은 직접 올립니다. 그래야 릴리스할 때만 사용자 쪽이 바뀝니다.

| 올림 | 언제 |
|---|---|
| patch | 고침, 표현 정리, 동작이 안 바뀌는 토큰 절감 |
| minor | 새 단계, 새 참조 파일, 새 동작 |
| major | 호출 이름이 바뀌거나 산출물 계약이 바뀔 때 |

바꾼 내용은 `CHANGELOG.md` 에 적습니다. 플러그인 사이에 버전 제약을 걸 거라면
`<플러그인>--v<버전>` 형식으로 태그하세요. `claude plugin tag --push` 가 매니페스트를 보고 태그
이름을 만들어 줍니다. 저장소에 git 사용자 정보가 없으면 이 명령이 실패하니 `git config user.name`
과 `user.email` 을 먼저 설정해 두세요.

## 스킬 시험하기

실제 대상에 한 번 돌려 보는 것 말고는 방법이 없습니다. 그렇게 해야만 드러나는 것이 셋입니다.

1. **멈춰야 할 곳에서 멈추는가.** 지시문에 그렇게 적혀 있다는 건 증거가 아닙니다.
   `link-design-pitch` 의 첫 실행은 자기가 적어 둔 정지 지점을 지나쳐, 아무도 요청하지 않은
   상세 문서를 만들어 냈습니다.
2. **측정이 맞는 것을 재고 있는가.** 잘못된 결론 두 개가 여기서 나왔습니다. 한쪽에서는 강조된
   요소를, 다른 쪽에서는 평범한 요소를 재서 비교한 것. 그리고 대비 계산이 `rgb()` 만 읽을 줄
   알아서 `#FFFFFF` 를 검정으로 처리한 것.
3. **결과물이 예상한 모양인가.** 숫자만 보지 말고 실제로 그려진 것을 눈으로 보세요. 테두리가
   안 보이는 카드는 누가 대비를 계산하기 훨씬 전에 이미 고장으로 보입니다.

**이미 답을 아는 프로젝트에는 돌리지 마세요.** 방금 직접 설계한 것에 스킬을 돌리면 잘 되는 것처럼
보입니다. 하지만 그건 판단을 당신이 공급하고 있는 것이지 스킬이 하고 있는 게 아닙니다.

## 이미 겪은 것들

같은 곳에서 두 번 넘어지지 않기 위한 목록입니다. 전부 실제로 일어났습니다.

### 스킬 내용

1. **정지 지점을 지나쳤습니다.** 지시문에 「여기서 멈추고 물어라」라고 적어 두었는데도 그대로
   상세까지 만들었습니다. 멈추는 단계는 **왜 멈춰야 하는지를 함께 적고, 문서에서 두 번 말하세요.**
2. **한 문장이 결과를 편향시켰습니다.** 「답은 흔히 둘의 조합이다」 때문에 서로 다른 두 프로젝트가
   연달아 조합으로 끝났습니다. 스킬이 매번 같은 결론을 내면 그렇게 만든 문장을 찾으세요.
3. **패턴을 무조건 추가하려 했습니다.** 에이전트 관련 상태를 모든 실행에 넣으려다, 대시보드에는
   필요 없다는 지적을 받았습니다. **조건을 먼저 정하고, 안 맞으면 아예 안 나오게** 하세요.
4. **위계를 면적으로만 재려 했습니다.** 대비로 위계를 만드는 설계에서는 면적비가 1.00배인 게
   정상입니다. 무엇이 위계를 나르는지 먼저 정하고 그것을 재세요.

### 비용

5. **참조 파일 전체를 매번 읽었습니다.** 24개 중 10개만 쓰는데 24개를 읽고 있었습니다. 색인을
   붙여 3,300 토큰을 줄였습니다.
6. **색인을 만들고 스킬에 안 알렸습니다.** 파일 구조만 바뀌고 읽는 양은 그대로였습니다.
   **「색인을 먼저 읽어라」를 SKILL.md에 반드시 적으세요.**
7. **색인을 자동 요약하려 했습니다.** 판단을 가르는 구절이 문장 중간에서 잘립니다. 손으로 쓰세요.

### 측정

8. **방향마다 다른 요소를 재고 비교했습니다.** 한쪽은 강조된 카드, 다른 쪽은 평범한 카드를 재서
   4.28 대 1.15라는 무의미한 숫자가 나왔습니다. `:not()` 으로 명시해서 고르세요.
9. **대비 함수가 hex를 못 읽었습니다.** `rgb()` 만 파싱해서 `#FFFFFF` 를 검정으로 계산했습니다.
   그럴듯한 숫자가 나와서 한참 못 봤습니다.
10. **opacity를 합성하지 않고 쟀습니다.** 반투명 요소는 화면에 그 색으로 있지 않습니다.
    배경과 합성한 뒤 재야 합니다.
11. **viewport가 0인 정적 스냅샷을 믿었습니다.** 거기서 나온 폭·넘침 값은 전부 가짜입니다.
    `clientWidth` 를 먼저 확인하세요.
12. **숫자만 보고 렌더된 것을 안 봤습니다.** 테두리가 안 보이는 카드는 대비를 계산하기 훨씬 전에
    이미 눈으로 보입니다.

### 산출물

13. **산출물 폴더를 스킬 이름으로 지었습니다.** 남의 저장소 최상위에 `design-pitch/` 가 생겨
    나중에 옮겨야 했습니다. 폴더 이름은 **안에 든 것**으로 짓습니다.
14. **계획 문서를 최신화하지 않았습니다.** 이미 구현이 끝났는데 문서가 「미착수」라고 적혀 있어
    같은 작업을 다시 시킬 뻔했습니다. 계획을 읽기 전에 코드가 이미 그렇게 돼 있는지 보세요.
15. **PNG를 「AI용」으로만 봤습니다.** GitHub도 메신저도 휴대폰도 HTML을 렌더하지 않습니다.
    사람이 보는 자리도 대부분 PNG입니다.

### 배포

16. **`description` 에 콜론+공백을 넣었습니다.** frontmatter 전체가 조용히 사라져 스킬이 발동하지
    않는 상태가 됐습니다. `claude plugin validate` 가 잡았습니다. 커밋 전에 반드시 돌리세요.
17. **개인 이메일이 커밋 8개에 박힌 채 공개됐습니다.** 공개 전에 `git log --format="%ae"` 로
    확인하고, 필요하면 GitHub noreply 주소로 이력을 다시 쓰세요.
18. **사내 프로젝트명이 커밋 메시지에 남았습니다.** 파일만 검사하고 이력을 안 봤습니다.
    **푸시 전에 파일과 커밋 메시지를 모두** 훑으세요. 푸시한 뒤에는 정리가 지저분해집니다.
19. **`gh repo create` 가 실패해도 저장소는 만들어집니다.** 원격이 연결되지 않은 채 빈 저장소만
    남았고, 그래서 마켓플레이스 등록이 실패했습니다. 만든 뒤 `git remote -v` 로 확인하세요.
20. **`claude plugin tag` 가 git 사용자 정보 없이 실패합니다.** 저장소에 `user.name` 과
    `user.email` 을 먼저 설정해 두세요.

### 문서

21. **영어로 썼습니다.** 사용자가 한국인인데 대문이 영어였습니다. 사람이 읽는 문서는 한국어,
    모델이 읽는 문서는 영어입니다.
22. **이모지를 넣었습니다.** 빼세요.
23. **소개가 스킬 내용과 모순됐습니다.** 「만들기 전에 정하는」이라고 써 놓고, 정작 스킬은 작업 중과
    이미 출시된 것에도 쓰도록 만들어져 있었습니다. 소개를 쓸 때 본문을 다시 읽으세요.


## 문서 언어

사람이 읽는 문서(README · AUTHORING · CHANGELOG · AGENTS)는 한국어로 씁니다.

`SKILL.md` 와 `references/` 는 영어로 씁니다. 읽는 것이 모델이고, 같은 내용이라도 영어일 때
토큰을 덜 쓰기 때문입니다. 대신 스킬 안에 **「사용자가 쓰는 언어로 답하라」** 를 반드시 넣으세요.
이게 없으면 영어로 답하기 시작합니다.
