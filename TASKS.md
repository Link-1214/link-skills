# 진행 중 작업

## 2026-08-18 — 두 디자인 스킬, 실제로 돌려보고 고침

**상태:** 완료. `1.8.0` 으로 올리고 푸시했습니다.

### 무엇을 했나

기준 문서에 대고 전수 감사한 결과 45 건을 고쳐 봤다가 **되돌렸습니다.**
`detail/SKILL.md` 가 +116% 로 불고 진입 검사가 84 줄이 되어, 「무엇을 만드는가」보다
「언제 거부하는가」가 길어졌기 때문입니다. 이 스킬의 목적은 좋은 방향 열 개를 걸어 놓고
소유자가 고르게 하는 것이지 관문이 아닙니다. 그 작업은 `fix/design-pitch-audit` 브랜치에
발견 목록으로 남겨 뒀습니다.

대신 이 저장소가 원래 하던 방식 — **실제로 돌려보고 나온 것만 고치기** — 로 다시 했습니다.

| 커밋 | 근거 |
|---|---|
| `cc4151b` | 겪은 것 9 건 (구버전 기록 거부, 비브라우저 측정 수단 부재, 대비 12 건, 없는 CLI 안내 등) |
| `59d3a88` | pitch 1 차 실행 — 열 개가 일곱으로 읽힘 |
| `d466a17` | 2 차 — 여덟으로. 남은 두 쌍 처리 |
| `69ea371` | 3 차 — 아홉으로. 쌍 문제를 개별 대신 규칙으로 |

### 실행 3 회의 궤적

| | 1 차 | 2 차 | 3 차 |
|---|---|---|---|
| 실행자 판정 | 「일곱쯤」 | 「8 개, 2 쌍 약함」 | 「열 개가 열 개로, 한 쌍 빼고」 |
| 뭉친 것 | 3 장 한 덩어리 | 2 쌍 | 1 쌍 |
| `--t2` 카탈로그 이탈 | 8 개 | 0 | 0 |
| 스크린샷 | 절반 유실 | 정상 | 정상 |

`--t2` 가 가장 깨끗한 증거입니다. 2 차 실행자가 하나를 올렸다가 **실측이 통과라서 카탈로그
원값으로 되돌렸습니다.**

### 파일별 증감 (`e1057ff` 대비)

```
SKILL.md         265 → 265  (+0)     ← 네 라운드 내내 손대지 않음
directions.md    460 → 519  (+59)
feasibility.md   146 → 173  (+27)    ← 엑셀·마크다운 절 신설
board.md         211 → 235  (+24)
detail/SKILL.md  152 → 159  (+7)
detail/spec.md   106 → 126  (+20)    ← 비브라우저 측정 절 신설
```

**절차 파일은 처음부터 멀쩡했습니다.** 틀린 것은 전부 데이터와 참조 자료였습니다 —
대비값, 굵기 정의, 다루지 않던 대상, 서로 충돌하는 측정 지시.

### 안 한 것

- **차트를 어떻게 그릴지는 스킬 전체에 한 줄도 없습니다.** 실행자 표현으로 「제목 획 하나에
  15 줄, 차트 생성기 프로젝트의 주제 전체에 0 줄」. 절을 새로 쓰는 일이라 규모가 다릅니다.
- Phase 1 에서 소유자가 없을 때 멈추는지 진행하는지 — 세 실행 모두 「진행」으로 해석했고
  모두 산출물을 냈지만, 명시가 없습니다.
- 프레임워크가 플랫폼 위에 올라앉은 경우(Streamlit·Shiny·WordPress). 「목록에 없는 대상」은
  다루는데 이 경우는 다루지 않습니다.

### 시험 중 나온 저장소 밖 발견

`exercises/hive-dashboard-core` 의 출하된 다크 팔레트에서 11px 텍스트가 3.37:1
(`text_hint #5F726D`), 12px 가 4.44:1. 기록의 7.05:1 은 HTML 보드에서 잰 값이고 실제 사용
토큰은 그 아래 두 단계였습니다. 그쪽 저장소 일입니다.
