# link-skills — AI 에이전트를 위한 안내

이 저장소는 스킬 모음입니다. 스킬은 **글로 쓴 작업 절차**이고, 어떤 코딩 에이전트든 그대로 따라갈
수 있습니다. Claude 전용 도구는 하나도 쓰지 않습니다. 설치할 필요 없이 `SKILL.md` 를 읽고 따르면
됩니다.

## 용어

| 용어 | 뜻 |
|---|---|
| 마켓플레이스 | 이 저장소. `add` 하는 대상 |
| 플러그인 | 설치 단위. 스킬 여러 개를 담음 |
| 스킬 | 호출 단위. `SKILL.md` 하나 |

설치는 플러그인 단위입니다. 한 플러그인 안의 스킬은 따로 받을 수 없습니다. 서로 없으면 의미가
없는 스킬은 그래서 한 플러그인으로 묶습니다.

## 이 저장소의 스킬

| 스킬 | 진입점 | 언제 부르나 |
|---|---|---|
| link-design-pitch | `plugins/link-design-pitch/skills/link-design-pitch/SKILL.md` | 산출물의 시각 방향이 안 정해졌을 때. UI, 리스타일, 테마, 색 체계, 발표 자료, 보고서, 스프레드시트. 또는 지금 것이 밋밋하다는 말이 나왔을 때. 시안 10개 중 어느 것으로 갈지 묻고 끝납니다. |
| link-design-pitch-detail | `plugins/link-design-pitch/skills/link-design-pitch-detail/SKILL.md` | 방향이 이미 정해졌고 **실제 물건이 만들어졌을 때**. 모든 화면에 적용하고 인터랙션 선택지를 제시합니다. 정해진 방향이 기록에 없으면 실행을 거부합니다. |

둘은 하나의 흐름이고, 사이에 사람이 만드는 단계가 있습니다.

```
질문 5개 → 시안 10개 → 추천 → [사용자가 고름]
                                    ↓
                  [사용자가 실제 기능과 내용을 만듦]
                                    ↓
     모든 화면에 적용 → [인터랙션 선택지 제시] → [사용자가 고름]
```

각 스킬 폴더에 자체 `AGENTS.md` 가 있습니다. 단계별 상세와 어떤 참조 문서를 언제 읽는지가 거기
적혀 있으니, 전체 트리를 훑기 전에 그것부터 읽으세요.

## 프로젝트에 붙이는 법

받아 둔 경로를 프로젝트의 `AGENTS.md` 에 적어 둡니다.

```markdown
시각 방향이 안 정해졌다면
<받은경로>/link-skills/plugins/link-design-pitch/skills/link-design-pitch/SKILL.md 를 따른다

방향이 정해졌고 실제로 만들어졌다면
<받은경로>/link-skills/plugins/link-design-pitch/skills/link-design-pitch-detail/SKILL.md 를 따른다
```

## 지켜야 하는 두 가지

**모든 스킬은 대상 프로젝트에 결정 기록을 마크다운으로 남깁니다.**

- **다시 하기 전에 기록부터 읽으세요.** 이미 기록이 있다면, 한 세션을 들여 답하려던 질문에 답이
  이미 나와 있을 수 있습니다.
- **기록을 최신으로 유지하세요.** 낡은 기록은 없느니만 못합니다. 다음 에이전트가 그걸 믿기
  때문입니다. 계획으로 적힌 것을 구현했으면 구현했다고 고치세요. 이걸 안 해서 실제로 같은 작업을
  두 번 한 적이 있습니다.

**정해지지 않은 것을 만들지 말고 선택지를 제시하세요.** 두 스킬 모두 결정 지점에서 멈춥니다.
사용자가 방식을 고르기 전에 명세를 다 써 두면, 제시하지 않았던 답이 나오는 순간 전부 버려집니다.
실제로 한 번 그랬습니다.

이것은 「모를 때 추측을 숨기지 않는다」를 디자인 스킬에 맞춰 쓴 것입니다. 유형이 다른 스킬이
들어오면 모습도 달라지므로, 이 저장소에 스킬을 새로 만들거나 기존 스킬을 검토할 때는
[`AUTHORING.md`](AUTHORING.md) 의 「만들기 전에 정할 것」 절을 먼저 읽으세요. 공통으로 지킬 것
둘과, 스킬이 스스로 정하는 계약 한 줄, 검토용 질문 다섯이 거기 있습니다.

## 저장소 구조

```
link-skills/
├── .claude-plugin/marketplace.json     Claude Code 마켓플레이스 카탈로그
└── plugins/
    ├── link/                           전체 설치용. 의존성만 있고 스킬은 없음
    └── link-design-pitch/              플러그인 하나, 스킬 둘
        ├── .claude-plugin/plugin.json
        ├── README.md                   사용법
        └── skills/
            ├── link-design-pitch/            SKILL.md · AGENTS.md · references/
            └── link-design-pitch-detail/     SKILL.md · AGENTS.md · references/
```

`.claude-plugin/` 디렉터리와 `plugins/` 중첩은 Claude Code 설치기를 위한 구조입니다. 다른
에이전트라면 무시하고 위의 `SKILL.md` 경로로 바로 가면 됩니다.

## 언어

이 문서를 포함해 사람이 읽는 문서는 한국어입니다. `SKILL.md` 와 `references/` 는 영어인데,
읽는 것이 모델이고 영어가 토큰을 덜 쓰기 때문입니다. **출력은 영어로 나오지 않습니다.**
스킬 안에 사용자가 쓰는 언어로 답하라는 지시가 들어 있습니다.
