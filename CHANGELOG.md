# 변경 이력

버전은 각 플러그인의 매니페스트에 적혀 있고 직접 올립니다. 릴리스할 때만 바뀝니다.
고침은 patch, 새 동작은 minor, 호출 이름이나 산출물 형식이 바뀌면 major 입니다.

## link-skill-packer

### 1.0.0

사내에서 깃허브를 못 쓰는 상황에서 나왔습니다. 외부에서 만든 스킬을 사내로 들여오는 것은 되는데,
사내에서 만든 스킬을 옆자리 동료에게 주는 방법이 없었습니다. 원래 경로인 플러그인 배포가 깃
저장소를 전제로 하기 때문입니다.

**압축이 아니라 검사가 값어치입니다.** zip으로 묶는 것 자체는 명령 한 줄이라 스킬이 필요 없습니다.
필요한 이유는 받는 쪽에서 스킬이 고장 나는 방식 중에 **오류 메시지가 없는** 것들이 있어서입니다.
BOM, 폴더 이름과 다른 `name`, 따옴표 없는 `description` 안의 콜론+공백 — 전부 frontmatter가 통째로
버려져 스킬이 목록에 아예 안 뜹니다. 받은 사람은 「안 되는데요」라고만 말할 수 있고, 원인은 보내는
쪽에서 몇 초면 잡힙니다. 이 중 콜론+공백은 이 저장소에서 실제로 한 번 일어났습니다.

**계약은 두 조건을 모두 겁니다.** 검사를 통과하고, 담길 내용을 만든 사람이 확인해야 zip을 씁니다.
스크립트가 앞의 절반을 강제하고(`check`와 `build` 양쪽에서 같은 검사를 돌리므로 `build`를 직접
불러도 못 건너뜁니다), 뒤의 절반은 `SKILL.md`의 정지 지점입니다. 사내 시스템 이름이나 실수로 들어온
작업 파일을 알아볼 수 있는 사람은 만든 사람뿐이고, 공유 드라이브에 올라간 zip은 초안이 아닙니다.

**설치된 스킬 81개에 돌려 보고 차단 검사 둘을 뺐습니다.** 저장소 안에서만 시험할 때는 안 보였습니다.

`name` 이 폴더명과 다르면 차단하고 있었는데, **틀린 판단이었습니다.** `taste-skill` 폴더의
frontmatter는 `design-taste-frontend` 인데 그 스킬은 지금 `taste-skill` 이라는 이름으로 멀쩡히
목록에 떠 있습니다. 그런 스킬이 5개 있고 전부 정상 동작합니다. Claude Code는 **폴더명으로** 호출하고
frontmatter 이름은 그걸 막지 못합니다.

여기 더 나쁜 게 딸려 있었습니다. 패커가 zip 안 폴더를 frontmatter 이름으로 만들고 있어서,
`taste-skill` 을 포장하면 받는 쪽에 `design-taste-frontend` 로 설치됐습니다. **옮기라고 만든 도구가
이름을 바꾸고 있었습니다.** 폴더명을 기준으로 고쳤습니다.

「참조 파일이 없음」도 차단에서 뺐습니다. 81개 중 **13개가 막혔고 전부 멀쩡한 스킬**입니다.
`writing-skills` 는 스킬 쓰는 법을 설명하는 문서라 링크가 전부 예시이고, `domain-modeling` 은
`./src/ordering/CONTEXT.md` 처럼 사용자 프로젝트 경로를 마크다운 링크로 적습니다. 문법으로는 진짜
깨진 참조와 구분되지 않으므로 질문으로 내렸습니다. 차단은 이제 파일만 보고 확실히 판정되는 다섯
가지뿐입니다 — `SKILL.md` 없음, BOM, frontmatter 블록 없음, `description` 없음, 콜론+공백.

**튀는 파일 판별에서 배치 규칙 둘을 뺐습니다.** 「최상위에 떠 있음」과 「스킬 구조에 없는 폴더」가
81개에서 137건 중 136건을 만들었습니다. `understand` 하나가 49건입니다. 위험을 잡으라고 만든 규칙
(데이터 파일, 초안 이름)은 **0번** 발동했습니다. 배치 규칙은 「이 파일이 나가면 안 된다」가 아니라
「이 사람이 내가 가정한 폴더 구조를 안 썼다」를 재고 있었고, 그 목록은 이 필터가 없애려던 바로 그
안 읽히는 목록입니다. 지운 뒤 81개 전체에서 1건으로 줄었고, 시험용 위험 파일 4개는 그대로 잡힙니다.

**안 풀리는 경로를 문서 단위로 묶었습니다.** `workspace-code-style` 하나가 질문 14줄을 뱉었습니다.
전부 사용자 프로젝트 경로였고요. 이제 문서마다 한 줄이고, 그 문서의 경로가 **전부** 안 풀리면
그렇다고 말합니다 — 깨진 참조가 아니라 바깥 얘기를 하는 문서라는 뜻이라, 답이 목록에서 예/아니오로
바뀝니다.

**차단 기준이 과해서 멀쩡한 스킬 둘을 막았습니다.** 저장소의 스킬 넷 전부에 돌려 보고 나서야
드러났습니다. `link-design-pitch` 는 `` `<output>/01-directions.png` `` 를, `link-design-pitch-detail`
은 `` `docs/design/DECISION.md` `` 를 언급하는데, 앞의 것은 스킬이 **실행 중에 만드는** 경로이고 뒤의
것은 **사용자 프로젝트 안에** 있는 파일입니다. 둘 다 동봉 파일이 아닌데 「참조하는 파일이 없음」으로
차단됐습니다. 우회 수단이 없어서 아예 포장이 불가능한 상태였습니다.

둘을 나눠 고쳤습니다. 자리표시자가 든 경로(`<>`, `{}`, `$`, `%` 등 — 윈도우가 파일명에 금지하는
문자들)는 애초에 파일일 수 없으므로 검사 대상에서 뺐습니다. 그리고 **차단은 마크다운 링크에만**
겁니다. `[글](경로)` 는 「이 파일이 여기 있다」는 저자의 단언이지만, 백틱 경로는 그냥 경로 얘기이고
스킬 문서는 자기 번들만큼이나 사용자 프로젝트 얘기를 합니다. 백틱 경로가 안 풀리면 이제 질문으로
나갑니다 — 「이건 동봉 파일입니까, 사용자 프로젝트의 경로입니까」.

실제 스킬에 돌려 보지 않았으면 못 찾았을 것입니다. 만들어 둔 시험용 폴더는 전부 제가 규칙을 알고
만든 것이라 걸리지 않았습니다.

**파일 목록을 전부 보여주던 것을 그만뒀습니다.** 「나가면 안 되는 파일이 있나」를 물으면서 열네 줄을
늘어놓는 것은 판단을 사람에게 떠넘긴 것입니다. 아무도 안 읽고, 읽어도 `references/guide.md` 가
위험한지는 이름만으로 모릅니다. 이제 통상적인 것은 세기만 하고 틀에서 벗어난 것만 이유와 함께
보여줍니다. 넷을 봅니다 — 데이터 파일 확장자, 초안·임시·사본으로 읽히는 이름(한국어 포함), 스킬
구조에 없는 폴더나 최상위에 떠 있는 파일, 그리고 **아무도 링크하지 않는 마크다운**입니다.

마지막 것이 값어치가 큽니다. 마크다운은 `SKILL.md` 에서 도달하게 되어 있으니, 아무도 안 가리키는
마크다운이 곧 흘러든 파일의 모양입니다. FAIL 검사에서 이미 참조를 뽑고 있었으므로 그것을 뒤집기만
하면 됐습니다. 시험용으로 만든 지저분한 폴더에서 파일 10개 중 4개가 걸렸고, 그중 하나가
`references/고객명단.md` 였습니다 — 확장자로도 이름 규칙으로도 못 잡던 것입니다.

**`INSTALL.md` 가 자기모순을 쓰고 있었습니다.** 제가 안 만든 실제 스킬(`writing-skills`)을
포장해 보고 나왔습니다. 그 스킬은 `render-graphs.js` 를 담고 있는데, 안내서가 이렇게 나갔습니다.

```
## 필요한 것
따로 설치할 것은 없습니다. 문서 파일만 담겨 있습니다.

## 알아둘 것
이 스킬에는 실행되는 스크립트가 들어 있습니다 — render-graphs.js
```

한 절 아래에서 정반대를 말합니다. 원인이 둘이었습니다. `.js` 와 `.ts` 가 실행기 목록에 없어서
**Node.js 가 안 적혔고**, 실행기가 비었을 때의 기본 문구가 스크립트 존재 여부를 안 보고 「문서
파일만」이라고 단정했습니다. 뒤엣것이 더 나쁩니다 — `.bat` 만 든 스킬에서도 같은 거짓말이 나옵니다.

실행기에 Node를 넣고, 기본 문구를 세 갈래로 나눴습니다. 이제 스크립트가 하나라도 있으면 「문서
파일만」이라고 말하지 않습니다. `.bat`·`.cmd` 는 일부러 안 넣었습니다 — cmd.exe 는 윈도우에 늘
있으니 적으면 소음입니다.

import 스캔은 여전히 파이썬만 봅니다. `.js` 의 npm 패키지는 못 읽고, 그 한계를 `SKILL.md` 에
적었습니다. 언어마다 파서를 만드는 것보다 실행기 이름을 적는 쪽이 조용한 죽음을 막는 핵심입니다.

**설치해서 발동 시험을 돌렸더니 결함 넷이 더 나왔습니다.** 격리 환경에 실제로 설치하고, 스킬
이름을 대지 않은 프롬프트 넷을 던졌습니다. 셋은 정상 발동했고, 「이 스킬 description 좀 다듬어줘」
에서 **떠서는 안 되는데 떴습니다.** `link-skill-authoring` 몫인데 경합한 것입니다.

그 세션이 원인을 찾으면서 제 결함 셋을 같이 캐냈습니다.

- **`AGENTS.md` 심각도 표가 강등 이전 상태였습니다.** `name` 불일치와 참조 파일 누락을 아직
  FAIL로 적고 있었고 NOTE 행 자체가 없었습니다. Codex처럼 플러그인을 못 읽는 런타임은 이 파일을
  진입점으로 삼으므로, 거기만 코드와 다른 말을 하고 있었던 셈입니다.
- **`SKILL.md` 에 문단 세 개가 중복돼 있었습니다.** 질문 단계를 1단계로 합칠 때 옛 꼬리를 안
  지웠습니다.
- **`AGENTS.md` 가 `link-skill-authoring` 을 `../../../` 로 링크하고 있었습니다.** 패커가 남의
  스킬에서 경고하는 바로 그 패턴입니다. 이름 참조로 바꾸니 자체 검사 WARN이 1에서 0이 됐습니다.

description은 셋을 고쳤습니다. 대상을 「스킬」이라는 단어 하나에서 `SKILL.md` 가 든 폴더로 못
박았고, 트리거가 아니라 동작 설명이던 마지막 문장을 뺐고, **경합하던 이웃을 이름으로 지목해 경계를
그었습니다** — 「Not for writing or fixing what a skill says — that is link-skill-authoring」.
경계는 이렇습니다. 뜨긴 뜨는데 엉뚱할 때는 authoring, 복사해서 받은 게 아예 안 뜰 때는 packer.

**질문 단계를 없애고 대상 확인 하나로 합쳤습니다.** 이게 마지막이자 가장 큰 정리입니다.

처음에는 검사 뒤에 「판단할 것들에 답하라」는 단계를 따로 뒀습니다. 저장소의 다른 두 스킬이 멈추고
묻길래 같은 모양으로 만든 것인데, **잘못 베낀 것이었습니다.** 그 둘이 묻는 이유는 정답이 존재하지
않아서입니다 — 시안 열 개 중 무엇이 맞는지는 사용자의 취향이 곧 답이고, 스킬이 무엇을 약속하는지는
만든 사람만 답할 수 있습니다. 도메인이 없는 사람도 결과를 낼 수 있게 하려면 그 사람이 **가진
것**만 물어야 하니까요.

포장은 다릅니다. **정답이 있는 일이고 스크립트가 찾습니다.** 기계가 진짜로 모르는 것은 「이 파일이
나가면 안 되는가」 하나인데, 그건 취향이 아니라 사실이라 성격이 다릅니다. 그래서 질문 목록을 내미는
대신 **대상 확인 한 번**으로 합쳤습니다 — 「이 스킬이 맞습니까」에 눈에 걸리는 것을 같이 붙여서.
기밀 파일 문제는 거기서 자연스럽게 드러나고, 별도의 판단 단계는 사라집니다.

단계도 넷에서 둘이 됐습니다. 확인하고, 만듭니다.

이건 `link-skill-authoring` 이 경고하던 것이기도 합니다 — 「감사하는 스킬과 시안을 펼치는 스킬은
잘 만든 모습이 다릅니다. 하나의 체크리스트로 모든 스킬을 재면 안 맞는 항목을 말로 채우게 됩니다.」
그 말로 채운 항목이 억지 질문으로 나왔던 것이고, 아래 두 번의 정리는 증상 대응이었습니다.

**억지 질문 둘을 걷어냈습니다.** 설치된 스킬 81개로 재보니 36개가 멈추는데 **그중 진짜 문제는
0건**이었습니다. 판정 기준을 다시 댔습니다 — 답에 따라 다음 행동이 갈리는가.

- `name` 불일치(10건) — 포장은 어차피 폴더명을 씁니다. 뭐라고 답하든 같은 zip이 나오므로 질문이
  아닙니다.
- 한 문서의 경로가 **전부** 안 풀리는 경우(22건) — 전부 안 풀린다는 건 그 문서가 사용자 프로젝트를
  설명한다는 뜻이고, 22건이 22건 다 그랬습니다. 답이 늘 같으면 질문이 아닙니다.

둘을 참고 사항으로 내리니 **36 → 9건**이 됐고, 사라진 27개 중 진짜 문제는 하나도 없었습니다. 남은
9개에는 실제 건이 들어 있습니다 — `subagent-driven-development` 가 옆 스킬의
`../requesting-code-review/code-reviewer.md` 를 가리켜서, 혼자 포장하면 그 단계가 깨집니다.

**한 문서 안에서 일부만 안 풀리는 경우는 질문으로 남겼습니다.** 같은 파일의 다른 경로는 풀리는데
이것만 안 풀린다면 오타의 모양이기 때문입니다.

**정지 지점을 승인이 아니라 결정으로 다시 썼습니다.** 처음에는 목록을 보여주고 「예」를 기다리는
형태였습니다. 저장소의 다른 스킬은 전부 정답이 없는 것을 묻는데(시안 중 무엇, 인터랙션 어느 방식,
스킬이 무엇을 약속하는지) 이것만 승인 게이트였습니다. 승인 게이트는 답할 내용이 없으면 도장 찍기로
굳고, 그러면 검토한 것처럼 보이는데 실제로는 안 한 상태가 됩니다. 지금은 두 가지를 묻습니다 —
**이 목록에서 나가면 안 되는 파일이 있는가**, 그리고 **각 경고를 어떻게 읽어야 하는가.** 경고마다
판단해야 할 질문을 스스로 달고 나오고, 출력은 검사 종류별로 묶어 질문을 한 번만 적습니다.
멈춤을 늘리지는 않았습니다. 이 스킬은 일의 대부분에 정답이 있어서(BOM이 붙었는지는 판단이 아니라
사실) 멈춤 하나면 충분하고, 여러 번 물으면 정작 중요한 하나를 안 읽게 됩니다.

**답이 갈리지 않는 질문 하나를 걷어냈습니다.** 결정형으로 바꾸고 나서 「이 질문들에 정말 답할 거리가
있나」를 다시 봤더니, 실행 스크립트 항목이 억지였습니다. 만든 사람에게 「당신이 쓴 코드가 주장대로
동작합니까」를 묻고 있었는데, 답이 무엇이든 포장 절차가 안 바뀝니다. 그 항목의 값어치는 **받는
사람에게 알려주는 정보**지 보내는 사람에게 묻는 질문이 아니고, 이미 `INSTALL.md` 에 들어가 있습니다.
그래서 등급을 하나 더 뒀습니다 — FAIL은 막고, WARN은 답에 따라 다음 행동이 갈리고, NOTE는 결정할
것 없이 알려만 줍니다. 기준은 **답이 다음 행동을 가르는가** 하나입니다. 안 가르면 질문이 아닙니다.

**경고가 0건이면 질문도 하나로 줄어듭니다.** 「두 가지를 물어라」를 고정으로 두면 깨끗한 스킬에서
없는 질문을 지어내게 되고, 그러면 사용자가 질문을 안 읽기 시작합니다. 그래도 첫 질문(나가면 안 되는
파일이 있는가)은 항상 남습니다. **어떤 검사로도 닿지 않는 자리**라서입니다 — `고객사_실적.xlsx` 가
폴더에 있으면 경고는 0건입니다. 스캔 대상 확장자가 아니고, 파일 이름은 기계에게 글자일 뿐입니다.
가장 위험한 경우가 경고를 하나도 안 냅니다.

**부분 포장을 지원하지 않습니다.** 「이 파일은 빼고」가 자연스러워 보이지만, 제외 옵션을 쓰면 zip이
폴더와 달라집니다. 다음 사람이 옵션을 기억 못 해 다른 결과를 얻고, 만든 사람이 시험한 것과 나간
것이 달라집니다 — 자동 수정을 거부한 것과 같은 이유입니다. 뺄 것이 있으면 폴더에서 지우고 다시
돌립니다. zip은 언제나 폴더 그대로입니다.

**차단과 경고를 나눴습니다.** 차단은 전부 「받는 쪽에서 스킬이 아예 작동하지 않음」으로 한정했고,
기계가 확실히 판정할 수 있는 것만 넣었습니다. 절대경로나 비밀번호처럼 보이는 문자열은 정당한 경우가
있어 경고만 합니다. 폴더 밖을 가리키는 링크도 경고입니다 — 문서 링크가 깨지는 것과 스킬이 안 뜨는
것은 급이 다른데, 차단으로 두면 그런 링크를 가진 스킬을 아예 포장할 수 없게 됩니다. 실제로 이
저장소의 `link-skill-authoring` 이 그런 링크를 갖고 있습니다.

**발견한 문제를 고쳐 주지 않습니다.** BOM 제거 정도는 자동이 편하지만, 예외를 하나 두면 어디까지
손댔는지가 흐려집니다. 보내는 길에 고치면 만든 사람이 시험한 것과 실제로 나간 것이 달라집니다.

**zip에 담을 수 없는 것을 이름이라도 남깁니다.** 마크다운 외의 것도 정확히 옮기는지 확인하다가
빈 구멍을 찾았습니다. 한글 파일명과 바이너리는 바이트 그대로 왕복하는데(해시로 확인), 스크립트가
`pandas` 를 import하면 파일은 다 들어가도 그게 없는 컴퓨터에서 죽습니다. 그러면서 패커도
`INSTALL.md` 도 한마디를 안 했습니다. 이제 번들 안의 `.py` 를 `ast` 로 읽어 표준 라이브러리가 아닌
최상위 import를 뽑고, 필요한 실행기(python, PowerShell, bash)와 함께 `INSTALL.md` 의 「필요한 것」
절에 적습니다. 설치해 줄 수는 없지만, 받는 사람이 실행하기 전에 알 수는 있습니다.

**파일별 SHA-256을 매니페스트에 넣었습니다.** 「복사하라, 읽고 다시 쓰지 마라」는 지시일 뿐이라
지켜졌는지 확인할 방법이 없었습니다. 받는 쪽 Claude가 `SKILL.md` 를 친절하게 다시 저장하면 BOM이
붙고, 그 사실은 「스킬이 안 뜨는데요」라는 말로 며칠 뒤에 돌아옵니다. 이제 값이 달라져서 설치 직후
보입니다. 실제로 BOM을 붙여 재저장해 보니 값이 바뀌는 것을 확인했습니다.

여기서 하나 더 나왔습니다. `INSTALL.md` 에 넣은 확인 명령이 **깨진 채로 나갈 뻔했습니다.**
`Get-FileHash $_` 가 하위 폴더 파일에서 전체 경로 대신 파일명으로 넘어가 실패했습니다. 문서에 적는
명령도 실제로 돌려 봐야 합니다.

**한 줄에 여럿 있으면 첫 개만 세던 것을 고쳤습니다.** 이메일이 한 줄에 셋 있어도 하나만 잡혔고,
사설 IP도 마찬가지였습니다. 비밀 정보를 적게 세는 방향의 오차라 `finditer` 로 바꿨습니다. 이메일
정규식이 문장 끝 마침표까지 물던 것도 함께 고쳤습니다.

**BOM이 다른 문제를 가리던 것을 고쳤습니다.** 첫 실행에서 BOM 하나 때문에 frontmatter 파싱이 막혀
「frontmatter 블록 없음」만 나오고, 그 안의 이름 불일치와 콜론+공백은 안 보였습니다. BOM은 그대로
차단으로 보고하되 파싱은 BOM을 걷어낸 뒤 하도록 바꿔서, 네 문제가 한 번에 나옵니다. 안 그러면 하나
고치고 다시 돌려 두 개를 더 발견하게 됩니다.

`INSTALL.md`는 이 스킬이 없는 사람을 위해 쓰이므로 받는 쪽에는 아무것도 설치할 필요가 없습니다.
내용은 복사 목록이고, 받는 사람의 에이전트에게 명령을 실행시키는 지시는 넣지 않았습니다. 첨부
파일이 남의 에이전트를 조종하는 모양은 보내는 사람을 믿더라도 만들지 않는 편이 맞습니다.

## link-skill-authoring

### 1.0.2

원래 정리했던 일곱 기준과 대조해 보니 셋이 빠져 있었습니다. 그중 둘은 덜 옮긴 것이었고, 하나는
판단이 틀린 것이었습니다.

**안전성과 통제권을 뺀 근거가 틀렸습니다.** 워크스페이스 `AGENTS.md` 가 승인 경계를 이미 규정하니
중복이라고 봤는데, 그건 그 워크스페이스 얘기입니다. 이 스킬은 공개돼 있고 받아 쓰는 사람에게는 그
문서가 없습니다. 범용 기준을 만든다면서 로컬 사정에 기대 항목 하나를 지운 셈이라, 질문으로
되살렸습니다 — 묻지 않고 하는 일과 먼저 확인할 일은 무엇인가.

**재현성은 절반만 옮겨져 있었습니다.** 「결과가 틀렸는지 알 수 있나」는 묻고 있었지만 「무엇이
남아야 나중에 확인할 수 있나」는 빠져 있었습니다. 틀린 답을 알아보는 것과 그 답이 어떻게 나왔는지
되짚는 것은 다릅니다. 질문 2에 그 절반을 붙였습니다. 질문은 다섯에서 여섯이 됐습니다.

접근성은 되살리지 않았습니다. 핵심이던 「지식 부족을 숨기지 말고 끌어올린다」는 규칙 2가 이미
담고 있고, 남는 「자연어로 시작 가능」은 거의 모든 스킬에 해당해 기준 구실을 못 합니다.

**멈추는 지점을 넣었습니다.** 이 저장소의 스킬은 판단이 필요한 곳에서 멈추고 묻는데, 이 스킬만
그러지 않았습니다. 실제로 세 스킬의 계약을 사람에게 묻지 않고 지어냈습니다. 계약과 경계는 만드는
사람만 답할 수 있으므로, 대신 쓰면 이 스킬이 자기 규칙 2를 스스로 어깁니다.

멈춤은 한 군데뿐입니다. 사실과 판정은 그대로 하고, 아직 없는 약속을 정할 때만 묻습니다. 이미
적힌 계약을 검토하는 것은 판정이라 멈추지 않습니다. 전부 물으면 도구가 아니라 설문이 됩니다.

결정은 `SKILL.md` 의 계약 한 줄로 남으므로 별도 기록 파일은 두지 않았습니다.

### 1.0.1

실제 대상에 두 번 돌려 보고 나온 두 가지를 고쳤습니다. 발동과 산출물 품질은 문제가 없었고, 둘 다
문서가 실제 동작과 어긋난 쪽입니다.

**두 계층의 구분이 전달되지 않았습니다.** 「모든 스킬에 적용되는 규칙 둘」과 「스킬이 스스로 정하는
계약·질문」을 나눠 놓았는데, 두 실행 모두 넷을 평평한 목록으로 다뤘습니다. 이번에는 분석형 스킬이라
넷 다 해당돼 결과가 좋았지만, 참조형 스킬에 계약 한 줄을 억지로 붙이는 순간 이 스킬이 막으려던 바로
그 일이 됩니다. 계층을 표로 명시하고, 아래 계층을 위로 올리지도 위 계층을 내리지도 말라고 적었습니다.

**`skill-creator` 와 함께 뜬다고 적어 두었는데 실제로는 함께 뜨지 않았습니다.** 스킬을 작성해 달라는
명백한 요청에서도 이 스킬만 떴습니다. description의 주장을 사실에 맞게 낮추고, 대신 본문에
「사용자가 실제로 만들거나 고치는 중이면 그 절차를 직접 호출하라」를 지시로 넣었습니다. 저절로 붙지
않으므로 이름을 불러 주는 것까지가 이 스킬의 일입니다.

### 1.0.0

스킬이 무엇을 보장해야 하는지 정하는 기준을 스킬로 만들었습니다. 스킬이 디자인 둘뿐일 때는 만들
때마다 판단하면 됐지만, 유형이 늘면 매번 다시 정하게 됩니다.

**공통 규칙을 둘로 줄였습니다.** 기준은 어겼을 때 조용히 실패하는가 입니다. 헷갈리게 쓰였거나
느린 스킬은 쓰다 보면 저절로 드러나므로 규칙이 필요 없습니다. 규칙이 값을 하는 자리는 틀렸는데
아무도 모르는 쪽뿐입니다.

- **필요할 때 뜨고, 아닐 때 안 뜬다.** `description` 은 트리거를 넉넉하게 쓰되 실행 순서를 담지
  않습니다. superpowers 가 측정해 The Description Trap 이라고 이름 붙인 것이 근거입니다.
  `description` 이 작업 순서를 요약하자 모델이 본문을 열지 않고 요약을 따라가, 본문에 두 단계로
  적힌 리뷰를 한 단계만 수행했습니다.
- **모를 때 추측을 숨기지 않는다.** 이 저장소 `AGENTS.md` 의 「정해지지 않은 것을 만들지 말고
  선택지를 제시하세요」가 이 규칙을 디자인 스킬에 맞춰 쓴 것입니다. 감사 스킬이면 검증하지 않은
  발견을 확정으로 보고하지 않는 것이고, 집계 스킬이면 결과를 바꾸는 가정을 출력에 남기는 것입니다.

나머지는 규칙 대신 둘로 뒀습니다. 스킬이 `SKILL.md` 에 스스로 적는 **계약 한 줄**과, 답을 정해
두지 않은 **질문 다섯**입니다. 유형마다 잘 만든 모습이 다른데 공통 기준을 늘리면 맞지 않는 항목을
억지로 채우게 됩니다.

**처음에는 이 내용을 `AUTHORING.md` 에 넣었다가 옮겼습니다.** 그 문서는 이 저장소에 스킬을
추가하는 절차라서, 범용 기준을 거기 두면 두 가지가 어긋납니다. 첫째로 플러그인 밖이라 설치한
컴퓨터에 파일이 가지 않습니다. `marketplace.json` 의 `source` 가 플러그인 폴더만 가리키므로
저장소 최상위 문서는 캐시에 들어가지 않습니다. 둘째로 범위가 다릅니다. 원칙은 남의 저장소에서
스킬을 만들 때도 그대로 통합니다. **문서를 어디에 두느냐가 곧 누가 읽을 수 있느냐입니다.**

## link-design-pitch

### 1.4.0

**시안 열 개가 서로 비슷하게 나오던 문제를 고쳤습니다.** 사용자가 다른 컴퓨터에서 실행했더니 열
개가 거의 같아 보인다고 알려왔고, 측정해 보니 사실이었습니다. 원인은 환경이 아니라 이 스킬에
있었습니다.

**무엇이 잘못되어 있었나.** 카탈로그의 토큰 라인이 색 여섯 개와 모서리 하나만 담고 있었고,
보드 제작 지침은 "방향마다 변수만 다시 정의하라"고 시켰습니다. 그래서 열 개 패널이 같은 서체,
같은 크기, 같은 자간, 같은 밀도로 그려지고 색만 달라졌습니다. 같은 파일이 다른 곳에서는 "이것들을
구별하는 건 대부분 타이포그래피"라고 적어 두고 있었으니, 스스로와 모순된 상태였습니다.

기존 보드 세 개를 렌더해서 잰 결과입니다. 열 패널 기준 서로 다른 값의 개수입니다.

| 채널 | 이전 |
|---|---|
| 실제 렌더된 서체 | **1** |
| 자간 | **1** |
| 버튼 여백 | **1** |
| 본문 굵기 | **1** |
| 패널 높이 | 1~7 |
| 배경색 | 8~10 |

눈으로 세어도 **열 칸에서 실제로 구별되는 디자인은 여섯 개**였습니다.

**무엇을 고쳤나.** 토큰 라인에 구조 값 여덟 개를 넣었습니다 — 서체 역할 `--f`, 굵기 `--fw`
`--hw`, 제목:본문 비율 `--sc`, 자간 `--ls`, 밀도 `--d`, 선 두께 `--bw`, 그림자 `--sh`.
`--d` 하나가 글자 크기·행간·여백·간격을 한꺼번에 움직이므로, 고밀도 방향은 실제로 빽빽해집니다.

**고정 높이를 금지했습니다.** 지침의 "같은 크기"를 모델이 `height:392px` 로 구현하고 있었는데,
그러면 밀도가 표현될 자리가 사라집니다. 고밀도와 미니멀이 같은 상자에 갇혀 있었습니다.
`min-height` 도 같은 함정이라 함께 막았습니다.

**서체는 런타임에 탐지하지 않습니다.** 역할 이름 여섯 개와 그것이 가리키는 실제 폰트 스택을
파일에 그대로 적어 두었습니다. 전부 Windows와 macOS 기본 폰트이고, 라틴 문자용과 CJK용을 짝지어
두었습니다. 웹폰트는 쓰지 않습니다 — 보드는 디스크에서 열려야 하고, 없는 폰트는 조용히 대체되어
캡션이 거짓말을 하게 됩니다. **그래서 패널에는 폰트 이름이 아니라 역할을 적습니다.**

**바꾼 스킬로 맹검 재실행해서 측정했습니다.** 같은 브리프, 같은 열 개 방향입니다.

| 채널 | 이전 | 이후 |
|---|---|---|
| 실제 렌더된 서체 | 1 | 3 |
| 글자 크기 | 2 | **7** |
| 행간 | 2 | **7** |
| 자간 | 1 | **6** |
| 패널 높이 | 7 | **9** |
| **눈으로 센 구별되는 디자인** | **6** | **8** |

실행한 에이전트의 보고 중 가장 중요한 문장은 이것이었습니다 — **"저 숫자들 중 하나도 제가 고르지
않았습니다."** 카탈로그가 값을 주면 복사되고, 산문으로만 적어 두면 채워지지 않습니다. 그것이 이
문제의 전부였습니다.

한글에 대해 정직하게 적어 둔 것: 한국어 핵심 폰트들은 고정폭 변형끼리 한글 글자체를 공유해서
실제로 구별되는 서체는 다섯 정도이고, 굵기도 두 단계로 뭉개집니다. 그래서 CJK에서는 굵기로
방향을 구별하지 말고 `--sc`·`--ls`·`--d` 로 가라고 적었습니다.

`feasibility.md` 에 새 값 여덟 개가 타깃별로 어디까지 살아남는지 표를 넣었습니다. Qt에는
`calc()` 도 사용자 정의 속성도 없어서, 브라우저 밖에서 토큰 라인은 붙여넣을 CSS가 아니라
손으로 옮길 명세입니다.

### 1.8.0

**같은 프로젝트에 세 번 돌려보고 나온 것들입니다.** Streamlit 차트 도구에 열 개 보드를 만들게
하고, 나온 결함을 고치고, 다시 돌리기를 세 번 했습니다. 실행자는 매번 결과를 보지 못한 상태로
시작했습니다. 그리고 별도로 PySide6 대시보드에 적용 스킬을 두 번 돌렸습니다.

측정한 것은 **눈으로 셌을 때 몇 개가 서로 다른 디자인으로 읽히는가** 하나입니다.

| | 1 차 | 2 차 | 3 차 |
|---|---|---|---|
| 열 칸에서 구별되는 디자인 | **7** | 8 | **9** |
| 카탈로그 `--t2` 를 손대야 했던 패널 | **8/10** | 0 | 0 |
| 스크린샷 | 절반 잘림 | 정상 | 정상 |

**흐린 글자색 열두 개가 지면 위에서 4.5:1 을 못 넘고 있었습니다.** 보드 템플릿이 바탕에는
`--g` 를, 카드에만 `--s` 를 칠하므로 카드 밖 라벨은 전부 지면에 앉습니다. 그런데 카탈로그는
표면 기준으로만 통과를 보증하고 있었습니다. 1 차 실행자는 고른 열 개 중 **여덟 개를 직접
올려야** 했습니다. 매번 고쳐 쓰는 값은 "그대로 복사하라"고 말할 수 있는 값이 아니므로,
경고를 다는 대신 **값 열두 개를 최소 폭으로 보정**했습니다(대부분 채널당 2~5 포인트, 최대는
21 번의 3.51 → 4.54). 2 차 실행자는 하나를 올렸다가 실측이 통과라 원값으로 되돌렸습니다.

**같은 아이디어를 두 칸에 쓰던 쌍들을 갈랐습니다.** 세 번 다 다른 쌍이 나왔습니다.

| 쌍 | 무엇이 같았나 | 고친 것 |
|---|---|---|
| 미니멀리즘 · 다크 | 한글 글자면 픽셀 해시가 **동일** | 8 번은 형태를 빌리는 항목인데 빌릴 곳이 없었습니다. 기반을 지정하고 캡션에 밝히게 했습니다 |
| 스위스 · 바우하우스 | 토큰 15 개 중 7 개, 악센트가 RGB 거리 **17** | 바우하우스 악센트를 자기가 선언해 둔 파랑으로. 거리 382 |
| 문서 · 에디토리얼 | 바탕 16, 악센트 34, 흐린 글자 9 | 에디토리얼 악센트를 자기가 선언해 둔 스틸로. 거리 210 |

쌍을 하나씩 고치면 끝이 없어서, 실측으로 붙은 쌍들을 **선택 지침에 이름으로 적었습니다.**
보는 사람은 썸네일을 먼저 판단하고 거기서는 색이 전부입니다.

**굵기 표시가 두 가지를 겸하고 있었습니다.** "하드 의존성 **또는** 어떤 대상에서 실격시키는
실패"라 양방향으로 어긋나 있었습니다 — Liquid Glass 는 의존성이 있는데 표시가 없었고,
Material 은 없는데 있었고, 3D 는 의존성이 아니라 차트 경고를 굵히고 있었습니다.
`feasibility.md` 가 "Survives without it?" 에 *No* 라고 답한 것 하나로 좁혔습니다.

**초대해 놓고 받을 준비가 없던 대상 둘을 채웠습니다.** 스프레드시트는 질문 2 의 선지에도 토큰
생존 표에도 보드 지침에도 있는데 읽을 절이 없었고, 마크다운은 아예 없었습니다. 마크다운 절은
거절 조항입니다 — 렌더러가 스타일을 제거하므로 토큰 열다섯 개가 하나도 도달하지 않고, 래퍼
클래스 열 개는 같은 문서 열 장이 됩니다.

**적용 스킬이 구버전 기록을 거부할 뻔했습니다.** `Status` 필드가 생기기 전에 쓰인 기록에는
그 줄이 없는데, 진입 검사를 문자 그대로 읽으면 "선택되지 않음"으로 판정합니다. 그런 기록을
이어받는 것이 이 스킬이 하는 일의 대부분입니다. 필드 부재와 미선택을 구분하고, 방향이 실제로
정해졌는지를 내용으로 읽게 했습니다.

**"측정하라"고 하면서 브라우저가 아닌 대상을 재는 방법은 없었습니다.** 실행자가 Qt 앱을 재려고
`QT_QPA_PLATFORM=offscreen` 을 직접 지어냈습니다. 요구만 있고 수단이 없으면 조용히 건너뜁니다.
실측 방법과, 못 재면 그렇게 적으라는 탈출구를 넣었습니다. 함께 나온 것 둘: 위젯 트리에서
`PlotWidget` 과 그 안의 `QWidget` 이 같은 사각형을 보고해 2 위가 히어로의 자기 컨테이너가 되고,
창 크기에 따라 같은 화면의 면적비가 1.19 배에서 2.06 배로 움직입니다.

**참조 파일 둘이 정면으로 충돌하고 있었습니다.** 보드 지침은 서체를 canvas 에 다시 그려 잉크를
재라 하고, 카탈로그는 굵기가 핵심인 방향에 `-webkit-text-stroke` 를 sweep 하라 합니다. canvas
재렌더는 paint 시점 속성을 보지 못하므로, 둘을 그대로 따르면 아무리 올려도 숫자가 안 변하고
영원히 실패합니다. 스크린샷에서 재라고 명시하고, 굵기 주도 방향이 보드에 없으면 이 절차를
건너뛰어도 된다는 면제 조항을 붙였습니다.

**측정 함정 셋을 추가했습니다.** 배경이 `transparent` 이거나 `background-image` 로 칠해져
있으면 `getComputedStyle` 이 검정을 돌려주어 1.2:1 같은 그럴듯한 가짜 실패가 나옵니다.
`--user-data-dir` 을 재사용하면 프로필에 저장된 창 크기가 다음 실행으로 새어 `clientWidth` 가
1200 에서 764 로 조용히 바뀝니다. 그리고 스크린샷 높이는 예시값이 아니라 `scrollHeight` 를
재서 넣어야 하고, 고칠 때마다 다시 재야 합니다.

**"한 번에 검증"은 원리적으로 불가능해서 문구를 고쳤습니다.** 잉크 측정에 스크린샷이 필요하고
스크린샷 높이에 `scrollHeight` 가 필요한데 그건 첫 패스에서만 나옵니다. 실행자는 여덟 번
돌았습니다. 낭비인 것은 스크립트 여섯 개이지 실행 여섯 번이 아닙니다.

**차트가 앱 테마를 "거의 상속하지 않는다"는 대상에 따라 사실이 아닙니다.** Streamlit 은
`theme.chartCategoricalColors` 를 Plotly·Altair·Vega-Lite 에 적용합니다. 곧이 믿으면 "차트
테마를 코드로 다시 쓴다" 로 비용을 잡게 되는데 실제로는 설정 파일 세 줄입니다.

### 1.7.0

**세 대상에 실제로 돌려보고 나온 것들입니다.** 웹 랜딩·투자자 슬라이드·PySide6 데스크톱으로
각각 열 개 시안을 만들게 하고, 되읽기 단계까지 돌렸습니다. 방향은 스킬이 직접 골랐습니다.

| | 이름값을 하는 패널 | 거짓 캡션 |
|---|---|---|
| 웹 | 6 → 10 | 2 → 0 |
| 슬라이드 | 3 → 10 | 5 → 0 |
| Qt 데스크톱 | 2 → 6 | 7건 → 0 |
| **합계** | **11/30 → 26/30** | **14건 → 0** |

세 보드 모두 **모든 수치 검사를 통과한 상태에서** 저 결함들을 갖고 있었습니다.

**Qt 차단 목록에 두 속성을 추가했습니다.** `font-variant-numeric` 과 `text-transform` 이
QSS 에 없습니다. 전자가 특히 아팠습니다 — 숫자가 열을 이루는 화면은 전부 이걸 모르고 쓰고
있고, Qt 는 비례폭 숫자를 줍니다. 측정된 보드에서 **열 패널 중 여섯이 여기 기대고 있었고
선언한 곳은 하나도 없었습니다.** 대체 수단은 값 라벨의 폰트를 코드에서 지정하는 것입니다.

**스트로크 처방을 고정 숫자에서 목표로 바꿨습니다.** `1.8px` 은 어떤 보드에서는 맞고 어떤
보드에서는 부족했습니다 — 이겨야 할 기준선이 보드마다 다르기 때문입니다. 다른 패널이 400 이면
`1.8px`, 볼드를 쓰면 `2.6px` 이상이 필요했고 둘 다 실측입니다. 이제 **자기 보드에서 가장
무거운 평범한 패널을 실제로 넘길 때까지 올리라**고 적었습니다. 확인하지 않은 숫자는 처방이
아니라 짐작입니다.

**고정 프레임에서 잘리는 것을 검사하게 했습니다.** 슬라이드·인쇄물·앱 창은 내용이 넘치면
조용히 잘립니다. 오류도 스크롤바도 없고 다른 측정은 전부 통과합니다. 측정된 보드에서 두 패널이
16:9 아래로 지표를 흘렸고 한 패널은 제목 윗부분을 잃었습니다.

**실현가능 배지는 양쪽으로 틀릴 수 있다**고 적었습니다. 못 그리는 것을 「가능」이라 하는 것이
분명한 실패지만, 멀쩡히 그려지는 것을 「부분」이라 하는 것도 거짓이고 오너에게서 쓸 수 있었던
방향을 뺏습니다. **「부분」은 무엇이 막는지 이름을 대야 하고, 못 대면 그 배지가 틀린 것입니다.**

### 1.6.0

**되읽기 단계가 값을 한다는 것이 증명됐습니다.** 최종 스킬로 다시 실행했더니, 보드가 처음
쓰였을 때 **모든 수치 검사를 통과하면서 캡션 열 중 여섯이 화면에 없는 것을 주장**하고 있었습니다.
그중 벤토는 **격자가 아예 없었습니다** — `display:grid` 가 셀을 담지 않은 요소에 붙어 있어서
한 단짜리 카드 목록이 벤토라는 캡션을 달고 있었습니다. 요소 개수도, 대비도, 폰트 지표도,
패널 높이도 전부 깨끗했습니다. 산문을 그림과 대조하는 것 말고는 잡을 방법이 없는 종류입니다.

되읽기를 거친 뒤 최종 결과입니다.

| | 이전 | 지금 |
|---|---|---|
| 이름값을 하는 패널 | 4/10 | **6/10** |
| 거짓 캡션 | 3/10 | **0/10** |
| 육안으로 구별되는 디자인 | 7/10 | **10/10** |

패널 높이는 317px에서 1056px까지 벌어졌고, 어느 채널도 1에 머물지 않습니다.

**카탈로그가 자기 접근성 규칙을 어기고 있었습니다.** 스킬은 「흐린 글자가 가장 흔한 접근성
실패」라고 경고하면서, 정작 **29개 항목 중 17개**가 4.5:1을 넘기지 못하는 값을 담고 있었습니다.
측정된 보드에서 110개 텍스트 역할 중 45개가 실패했고, 세 패널은 **주요 버튼 라벨이 3.2:1
미만**이었습니다. 흐린 글자 `--t2` 열 개를 본문색 쪽으로 당겨 전부 통과시켰습니다. 강조색은
채우기용이 정당한 경우가 있어 값을 바꾸는 대신 **글자로 쓸 수 있는지 먼저 재라**는 규칙과,
버튼 라벨 색을 습관이 아니라 측정으로 고르라는 규칙을 넣었습니다.

**대비가 실패했을 때 무엇을 하라는 말이 어디에도 없었습니다.** 스킬이 대비를 재라고만 하고
그다음을 말하지 않아, 모든 지시를 지키면서 주요 버튼이 2.93:1인 보드가 나올 수 있었습니다.
이제 **여기서만은 카탈로그를 벗어나 값을 올리고, 무엇을 왜 바꿨는지 기록에 남기라**고 적었습니다.

**스트로크 처방값이 틀렸습니다.** 직전 판에서 굵기가 핵심인 방향에 `.6px` 를 쓰라고 했는데,
실측하니 한글 잉크 밀도가 0.211로 평범한 고딕의 0.294보다 **낮았습니다.** 브루탈리즘이 여전히
미니멀리즘보다 얇았다는 뜻입니다. 측정값을 함께 적고 **`1.8px`**(0.298)로 고쳤습니다.

산문과 토큰이 어긋나던 네 곳을 토큰에 맞췄습니다. 미니멀리즘의 보조글자·강조·테두리,
뉴모피즘의 면색, 고밀도의 글자 크기입니다 — 고밀도의 「12–13px」 은 자기 밀도값과 모순이라
밀도가 정하는 크기라고 고쳐 적었습니다.

### 1.5.0

**이름값을 하는지까지 확인했더니 아직 모자랐습니다.** "설명에 맞는 예시가 나오느냐"는 질문을 받고
최종 스킬로 다시 실행해 패널마다 카탈로그 설명과 대조했습니다. **열 중 넷만 온전히 자기 방향을
전달했고, 셋은 화면에 없는 것을 캡션이 주장하고 있었습니다.**

- **한국어에는 어느 기본 폰트에도 굵은 굵기가 없습니다.** `heavy` 역할은 다른 글자체를 고를 뿐
  더 굵게 만들지 못합니다. 실측하니 굴림이 맑은고딕보다 **가볍게** 그려져서, "굵은 그로테스크"라고
  적힌 브루탈리즘 패널이 바로 위 미니멀리즘보다 얇았습니다. 굵기가 핵심인 방향에는
  `-webkit-text-stroke` 를 쓰라고 적었습니다.
- **캡션 규칙을 고쳤습니다.** "폰트 이름 말고 역할을 적어라"가 오히려 거짓 캡션을 만들고
  있었습니다. 역할 이름은 스택의 라틴 쪽을 설명하는데, CJK 에서는 그냥 고딕으로 도착하기
  때문입니다. 이제 **쓰기 전에 렌더를 확인하고, 문자체계가 전달하지 못하면 타입에 대해 말하지
  않습니다.**
- **`measureText` 폭 비교는 CJK 에서 아예 작동하지 않습니다.** 한글은 고정폭 em 이라 어떤 서체든
  같은 폭이 나옵니다 — 열 패널이 전부 `672.00` 이었습니다. 픽셀로 래스터화해서 **잉크 경계로
  자른 뒤** 해시하라고 바꿨습니다.
- **항목 산문이 약속하는데 아무 데서도 주지 않던 속성들**을 구조 표에 넣었습니다. Y2K 의 베벨 창틀,
  에디토리얼의 내어쓰기와 비대칭 단, 재팬디의 질감, 뉴모피즘의 "버튼까지 돌출", 터미널의 고정폭이
  라틴 전용 약속이라는 사실.
- **Phase 2 에 되읽기 단계를 넣었습니다.** 토큰 라인은 패널을 그럴듯하게 만들지 충실하게 만들지
  않습니다. 각 항목의 산문을 자기 패널과 대조해서, 빠진 것을 넣거나 캡션에서 주장을 빼게 했습니다.
  부분 일치 여섯 개가 전부 여기서 나왔습니다.
- 카탈로그 안에서 **구조 서명이 겹치던 항목 8쌍**을 갈랐습니다. 그중 1 미니멀리즘 ↔ 3 모노크롬은
  실행 에이전트가 육안으로 지목한 바로 그 쌍이었습니다. 같은 서체·근접 구조·근접 바탕 충돌은
  이제 0쌍입니다.

### 1.3.0

스스로에게 적대적 오딧을 돌려서 나온 것들을 고쳤습니다. 여덟 개 중 여섯은 「글로는 맞는데 실행이
따라오는지 검증되지 않은」 부류였습니다.

**색인 절감이 실행에서도 성립하게 만들었습니다.** 「고른 열 개만 읽어라」는 지시만 있고 방법이
없었습니다. 파일을 여는 도구는 대개 통째로 돌려주므로, 그대로면 절감은 측정에만 존재합니다.
항목 제목(`## <번호>.`)을 검색해 그 줄 범위만 읽으라는 방법을 색인 옆과 본문 양쪽에 적었고, 줄
범위를 못 읽는 환경에서는 「한 번만 읽고 두 번 읽지 말라」로 후퇴하게 했습니다.

**질문 3이 아직 없는 답에 의존하던 모순을 풀었습니다.** 「다섯을 한 번에 물어라」와 「답 1·2에서
팔레트를 도출하라」가 같은 절차에 있었습니다. 맥락이 답을 담고 있을 때만 우연히 작동했습니다.
이제 팔레트는 맥락 추정으로 만들되 **어느 추정 위에 서 있는지 명시**합니다. 전제가 보이는 팔레트는
답과 같은 호흡에 교정됩니다.

**상세 스킬이 기록이 있는데도 거부하는 경우를 막았습니다.** 고정 경로 두 곳에 없으면 바로
거부했는데, 모노레포 하위나 다른 위치에 기록이 있으면 오작동 거부가 됩니다. 이제 프로젝트 전체를
한 번 찾아보고, 그래도 없으면 위치를 물은 다음에야 거부합니다. 거부 자체는 그대로 남습니다 —
기록이 정말 없으면 방향을 지어내지 않습니다.

**비용 숫자를 정직하게 만들었습니다.** 문서의 절감 수치는 읽기 비용만 센 것이었습니다. 실행
전체는 시안 열 개를 그리고 검증까지 하므로 그 몇 배가 듭니다. README에 그렇게 적었고, 이 스킬은
자주 도는 스킬이 아니라 큰 작업 전에 한 번 돌아 재작업을 없애는 스킬이라는 것도 함께 적었습니다.

**CI를 붙였습니다.** frontmatter의 콜론 함정, 마켓플레이스 등록 누락, 번들 의존성 누락, 이모지 —
전부 실제로 겪었거나 겪으면 조용히 실패하는 것들을 `scripts/validate_repo.py` 가 잡고, 푸시마다
돌아갑니다. 결함을 심은 저장소로 음성 테스트를 해서 일곱 종을 전부 잡는 것까지 확인했습니다.

그 밖에: 카탈로그 개수를 산문 여섯 곳에서 파일당 한 곳으로 줄였습니다(늘 때마다 여섯 곳을 고치다
하나 놓치면 문서가 거짓말을 합니다). PNG 캡처 명령의 Windows 변형을 적었습니다(작성자 본인 기계가
Windows인데 예시가 POSIX 전용이었습니다). 타깃이 브라우저가 아닐 때 보드는 근사라는 것을 보드에
말하게 했습니다. 비슷한 디자인 스킬이 여럿 설치된 환경에서는 이름으로 부르라고 README에
적었습니다.

### 1.2.0

**답에서 무엇이 켜지는지를 읽는 규칙을 넣었습니다.** 새 질문을 더하지 않았습니다. 다섯 개 답에
이미 신호가 들어 있기 때문입니다.

- 5번 답이 「조작하게」가 아니라 「알아채게·이해하게」이면, 화면이 숫자를 나열하는 대신 문장으로
  말해야 합니다. 데이터 스토리텔링이라고 부르는 것이고, 사실 이미 그렇게 하고 있었는데 이름이
  없어서 다음 실행에서는 안 나올 수도 있었습니다.
- 2번이나 5번에 제품이 사용자 대신 행동하는 대목이 있으면, 상세 스킬의 인터랙션 갈림길에
  「제품이 스스로 행동하는가」가 추가됩니다. 작업 중 표시, 제안과 적용의 구분, 제품이 바꾼 것과
  사람이 바꾼 것의 구분, 되돌리기 단위, 거부 사유입니다.
- **해당되지 않으면 그 갈림길은 아예 없습니다.** 대시보드·양식·문서 대부분이 여기 해당하고,
  거기서 이걸 묻는 건 소음입니다.

멀티모달 입력과 적응형 레이아웃은 검토하고 넣지 않았습니다. 전자는 니치이고, 후자는 4번 질문에서
자유롭게 말할 수 있어 따로 이름 붙일 값어치가 없습니다.

**PNG의 용도 설명을 고쳤습니다.** 「비전 되는 AI용」이라고 적어 두었는데 틀렸습니다. GitHub도,
메신저도, 휴대폰도 HTML을 렌더하지 않습니다. **사람이 이 보드를 보는 대부분의 자리가 PNG입니다.**
세 파일이 각자 다른 일을 하고 각자의 일에서 제일 싸다는 것도 함께 적었습니다 — HTML은 브라우저를
가진 사람용, PNG는 그 외 모든 곳, `DECISION.md` 는 재현용입니다.

### 1.1.0

카탈로그를 24개에서 29개로 늘렸습니다. 웹을 다시 훑어 빠진 것을 찾았고, 고전 셋과 2026년
흐름 둘이 나왔습니다.

- **바우하우스** — 원색과 기하 도형. 스위스·플랫·머티리얼의 조상이라 낯설지 않게 읽힙니다.
- **아르데코** — 기하학적 대칭과 금색. 격식이 필요한 자리용입니다.
- **구성주의** — 대각선과 적·흑. 긴급함을 만드는 데 특화돼 있습니다.
- **안티-AI / 핸드메이드** — 종이 질감, 잘린 가장자리, 의도적인 불완전함. 2026년 가장 큰
  흐름이고, AI가 만든 것처럼 보이면 안 되는 자리를 위한 것입니다.
- **자판디 / 따뜻한 미니멀** — 미니멀리즘의 실제 약점인 차가움을 온도로 해결합니다.

플랫 디자인은 검토했지만 넣지 않았습니다. 미니멀리즘·머티리얼·바우하우스 셋이 이미 그 영역을
덮고 있어서, 넣으면 색인만 길어지고 고를 때 헷갈립니다.

**목업을 어떻게 만드는지를 명시했습니다.** 이미지를 검색하거나 가져오지 않고 HTML과 CSS로
직접 그린다는 것. 전에는 산출물 경로와 「외부 요청 없음」 규칙에서 간접적으로만 드러났는데,
문서만 읽고는 헷갈릴 수 있었습니다. 검색해 온 이미지에는 그 프로젝트의 내용이 들어갈 수 없고,
보드가 시험하는 건 정확히 그것입니다.

### 1.0.0

첫 릴리스. 플러그인 하나에 스킬 둘을 넣었습니다. 두 번째는 첫 번째 없이는 의미가 없기 때문입니다.

**`link-design-pitch`** — 질문 다섯 개, 프로젝트의 실제 내용으로 렌더한 시안 열 개, 장단점과 렌더
가능 여부를 표로 정리, 추천 하나와 그 추천에 대한 가장 강한 반론까지. 그리고 **거기서 멈추고 주인의
선택을 묻습니다.**

**`link-design-pitch-detail`** — 정해진 방향을 모든 화면에 적용하고, 위계와 대비를 주장하는 대신
**재고**, 코드에서 뭘 바꿔야 하는지를 목록으로 냅니다. 그다음 인터랙션 선택지를 펼쳐 놓고 묻습니다.
**기록에 정해진 방향이 없으면 실행을 거부합니다.**

데스크톱 대시보드 하나와 웹 칸반보드 하나, 두 번의 실제 작업이 이 스킬의 모양을 잡았습니다.
아래 규칙이 유난히 구체적인 이유는 전부 거기서 나왔기 때문입니다.

- **정지는 필수이고 두 번 적혀 있습니다.** 첫 실행은 아무도 방향을 고르기 전에 상세 명세를 다
  만들어 버렸습니다. 결정 지점을 지나 만든 것은 주인이 버릴 수 있는 작업이고, 동시에 주인에게서
  빼앗은 결정입니다.
- **조합은 절대 열 개 중 하나가 아닙니다.** 초기 두 실행이 모두 조합으로 끝났는데, 지시문에
  「답은 흔히 둘의 조합이다」가 있었기 때문입니다. 조합에 슬롯을 주면 열 칸으로 아홉 개만 덮게 되고,
  빠지는 것은 가장 많이 가르쳐 주는 스트레치 안입니다.
- **위계는 재고, 어느 채널을 쓰는지도 함께 봅니다.** 첫 실행에서는 두 행을 걸친 요소 때문에 네 탭 중
  세 탭의 위계가 조용히 뒤집혀 있었습니다. 두 번째 실행에서는 같은 규칙이 멀쩡한 설계를 결함으로
  신고할 뻔했는데, 그 설계는 위계를 면적이 아니라 대비로 나르고 있었기 때문입니다.
- **대비는 불투명도 합성 뒤에 잽니다.** 남의 카드를 50%로 흐리는 건 세련돼 보이지만 3.5:1 로
  떨어집니다. 이 방식으로 접근성 결함 둘을 잡았고, 그중 하나는 이미 출시된 화면에 있었습니다.
- **5단계는 선택지를 제시하고 만들지 않습니다.** 「낙관적 갱신이냐 확인 후 반영이냐」를 축으로 쓴
  전체 거동 명세가, 아무도 제시하지 않았던 세 번째 답 하나에 한 문장으로 무효가 됐습니다.
- **산출물은 프로젝트가 문서를 두는 곳에 놓고**, 스킬 이름을 딴 폴더는 절대 만들지 않습니다.

이번 릴리스에 함께 들어간 것: 카탈로그 둘 다 앞에 손으로 쓴 색인이 있어 스물넷이 아니라 실제로
쓰는 열 개만 읽습니다(입력의 29% 절감). 보드는 PNG로도 캡처해 비전이 되는 에이전트가 마크업이
아니라 디자인을 실제로 보게 했습니다. `DECISION.md` 는 평문 마크다운이라 결정이 그것을 만든 세션보다
오래 남습니다.

## link

### 1.5.0

`link-skill-packer` 를 의존성에 넣었습니다. 이미 `link` 를 설치한 사람은
`claude plugin update link@link-skills` 로 받습니다.

### 1.4.0

`link-skill-authoring` 을 의존성에 추가했습니다. 이미 `link` 를 설치한 사람은
`claude plugin update link@link-skills` 로 받습니다.

### 1.0.0

전체 설치용 플러그인입니다. 스킬을 직접 담지 않고 나머지 플러그인을 의존성으로 가리킵니다.
한 번 설치하면 전부 들어오고, 새 스킬이 추가되면 업데이트로 받을 수 있습니다.
